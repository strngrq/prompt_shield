PySide6>=6.6,<6.9
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7,<4.0
# Russian lemmatizer backend used by spaCy's ru_core_news_* models.
# Bundled because the Russian model (downloaded at runtime) imports pymorphy3
# lazily and would otherwise fail on machines without it.
pymorphy3>=2.0
pymorphy3-dicts-ru
